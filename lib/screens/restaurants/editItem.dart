import 'package:flutter/material.dart';

class EditItem extends StatelessWidget {
  final doc;

  const EditItem({Key key, this.doc}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
